extends layout

block content
  main
    section(class="text-center")
      div(class='container')
        h1='Сократите ссылку'
        form(class="form-min")
          input(type="text" name="link" class="form-control" placeholder="Вставьте ссылку" class="link")
          button(class="btn btn-primary mt-2") Сократить
          

